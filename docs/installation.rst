.. _installation:

Installation
============

* To install pinax-points::

    pip install pinax-points

* Add ``pinax-points`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "pinax.points",
    )
